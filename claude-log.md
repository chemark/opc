# Claude 项目日志

记录本项目对话中有创意、有启发性的问答。

---

## 2026-05-04 | 用「项目日志文档」代替 memory 做项目内知识沉淀

**问题：** 新建一个项目级的 Claude 文档（项目日志文档），用来记录这个项目中遇到的所有比较有意思、有启发性的问题，以及你的解决方法和回答。在 CLAUDE.md 文档里加一个规则：每次遇到这种有创意、有启发性的问题，只要你回答并解决了，就把过程记录到那个项目日志文档里。

**回答/解决方案：** 
Memory 系统适合跨项目的用户偏好和全局反馈，但项目内部的知识沉淀放在 memory 里会和项目代码脱节。更好的做法是在项目内建一个 `claude-log.md`，和代码放在一起，通过 CLAUDE.md 中的规则驱动自动记录。

好处：①日志和项目文件同在 git 仓库，可以版本控制；②换设备/换 Claude 账号不丢失；③可以直接在 PR 或提交历史里看到决策背景；④不占用 memory 的索引空间。

---
